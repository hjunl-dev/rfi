# rfi — remote function invocation

Remote Function Invocation in Rust.

An educational RPC framework built with Rust, modeled after Java RMI.
The ultimate goal is a network synchronization SDK that functions via a single attribute—similar to Unity NGO's `[ServerRpc]` and `[ClientRpc]`.

## 🚀 Features

* 

### Example
